# Per-environment config holds real connection creds — never commit.
config.prod.sh
config.sh
# Generated by the scripts at runtime.
schema.sql
routines.sql
seqsync_all.sql
*.bak
# Local workdir / vercel-link dirs created at runtime.
workdir/
.vercel-link-*/
